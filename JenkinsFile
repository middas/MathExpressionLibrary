pipeline {
    agent any

    stages {
        stage('Build') {
            script {
                sh 'dotnet build'
            }
        }

        stage('Test') {
            script {
                sh 'dotnet test'
            }
        }
    }
}